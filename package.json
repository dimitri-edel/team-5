{
  "name": "team5-api",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "vite": "^5.0.8"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ]
} 